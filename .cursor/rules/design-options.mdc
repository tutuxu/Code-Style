---
alwaysApply: false
---
# 多方案对比（手动触发）

## 使用说明

1. 在对话中先描述需求（可 `@` 引用相关文件）
2. 输入：`@design-options`，并说明要对哪项需求做方案对比
3. 讨论并选定方案
4. 明确回复：`按方案 X 实现` / `采用推荐方案` 后，再进入编码

**触发方式**：仅在聊天中 `@design-options` 时生效（纯手动规则，勿自动套用）。

**适用场景**：新功能、重构、跨模块改造、表结构/API 变更、性能或架构取舍等。

---

## 触发后必须执行

### 1. 理解需求

- 用 2～4 句话复述：目标、边界、约束（性能、兼容、上线节奏、是否可改表等）
- 若信息不足，先追问 1～2 个关键问题，再列方案

### 2. 调研（只读）

- 只读检索与本需求相关的 Controller、Service、Dao/Mapper、XML、SQL、配置
- 对齐本项目惯例：Spring + MyBatis、`com.loit.modules.*` 分层、`Bicycle*` 等既有模式
- 在方案中引用具体路径或类名（便于对照）

### 3. 输出方案

- **至少 2 种**、通常 **3 种**；最多 **4 种**
- 若只能列 2 种，须简短说明为何不列更多
- 每种方案按下方「方案模板」填写，编号为 **方案 A / B / C / D**

### 4. 对比与推荐

- 给出简短 **对比小结**（列表即可，避免冗长表格）
- 标明 **推荐方案** 及 **3～5 条理由**（最小改动、可测试性、与存量一致、运维成本等）
- 若推荐依赖未确认前提（表结构、第三方接口、权限），**显式列出假设**

### 5. 协作选型

- 选项接近或依赖你的业务偏好时，使用 **AskQuestion**，列出方案 ID 供选择
- 回复末尾固定包含：**请确认采用哪一方案（或补充约束）后，我再开始实现。**

---

## 方案模板（每个方案必填）

- **名称**：一句话概括
- **思路**：改哪些层、数据流、关键步骤
- **优点 / 缺点**
- **影响范围**：模块、表、接口、是否破坏兼容
- **工作量**：小 / 中 / 大（粗估）
- **风险**：回滚、数据迁移、并发、运维等
- **与现有代码一致性**：是否复用既有 Service/Mapper/定时任务等模式

---

## 严格门禁（选型前禁止实现）

在用户**未明确**选定方案（例如「按方案 B」「采用推荐方案」）之前：

**禁止**

- 编辑、新建、删除项目内任何文件
- 执行会改变仓库或运行环境的命令（migrate、commit、安装依赖改 lockfile 等）
- 在未选型时直接写实现代码或代替用户做决定

**允许**

- 只读：搜索、Read、Grep、解释现有代码
- 与用户讨论方案、澄清需求
- 用户**已选定方案**后，可用 Plan 模式整理实现步骤（仍不写代码，直至用户确认开始实现）

若用户要求「直接做」，仍须先确认采用哪一方案；可复述推荐项并等待一句明确选型。

---

## 选型后的衔接

用户明确选型后：

1. 用 1～2 句话确认：采用的方案编号、范围与不做的事
2. 再按项目规范实现（命名 `@java-naming`，注释 `@java-javadoc` 等，按实际需要 `@` 引用）
3. 恢复正常的 Agent 实现流程
